{
  "name": "bidops",
  "private": true,
  "scripts": {
    "build": "npm run build --workspace=apps/web",
    "dev": "npm run dev --workspace=apps/web"
  },
  "workspaces": [
    "apps/web",
    "packages/db",
    "packages/types"
  ]
}
